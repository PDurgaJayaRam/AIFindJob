"""JOBFinder ingestion package (Phase 1).

A shared, multi-source job ingestion engine that fills one central `jobs` pool.
Each source is isolated and pluggable; one source breaking never breaks others.
"""
